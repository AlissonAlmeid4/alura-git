<template>
  <div class="q-pa-md">
    <div class="mensagem">
      <div class="text-h4">NOSSA HISTÓRIA!</div>

      <div class="text-h6">
        CONHEÇA OS DETALHES DA NOSSA CAMINHADA ATÉ O GRANDE SONHO DO NOSSO
        CASAMENTO.
      </div>
    </div>
    <div class="row justify-center">
      <div class="cover">
        <div class="book">
          <label for="page-1" class="book__page book__page--1">
            <img src="../photos/ilovepdf_images-extracted/capa.jpg" alt="" />
          </label>

          <label for="page-2" class="book__page book__page--1">
            <img src="../photos/ilovepdf_images-extracted/pag3.jpg" alt="" />
          </label>

          <!-- Volta para primeira pagina -->
          <input type="radio" name="page" id="page-1" />
          <!-- Avança para segunda pagina -->
          <input type="radio" name="page" id="page-2" />

          <!-- Segunda pagina -->
          <label class="book__page book__page--2">
            <div class="book__page-front">
              <img src="../photos/ilovepdf_images-extracted/pag1.jpg" alt="" />
            </div>
            <div class="book__page-back">
              <img src="../photos/ilovepdf_images-extracted/pag2.jpg" alt="" />
            </div>
          </label>
          <!-- Volta para primeira pagina -->
          <input type="radio" name="page" id="page-1" />
          <!-- Avança para segunda pagina -->
          <input type="radio" name="page" id="page-2" />
        </div>
      </div>
    </div>
  </div>
</template>
<style lang="scss">
.mensagem {
  text-align: center;
  padding: 12px;
  color: rgba(0, 0, 0, 0.7);
}
:root {
  /* spacing */
  /* this is what defines the global scale */
  --baseline: 12px;
  --base-size: var(--baseline) * 1.2;
}
* {
  box-sizing: border-box;
}
body {
  background-color: var(--body-bg);
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.cover {
  width: 48%;
  height: calc(var(--baseline) * 55);
  box-shadow: 0 0 100px rgba(0, 0, 0, 0.3);
}

.book {
  width: 100%;
  height: 100%;
  display: flex;
  perspective: 1200px;

  &__page {
    position: relative;
    width: 50%;
    height: 100%;
    display: grid;
    transform: rotateY(0deg);
    transition: transform 0.9s cubic-bezier(0.645, 0.045, 0.355, 1);
    transform-origin: 0% 0%;

    &--1 {
      cursor: pointer;
      overflow: hidden;
      img {
        width: 100%;
        max-width: 100%;
        height: 100%;
      }
    }

    &--2 {
      position: absolute;
      right: 0;
      pointer-events: none;
      transform-style: preserve-3d;
      background-color: var(--page-bg);
      background-image: linear-gradient(
        90deg,
        rgba(227, 227, 227, 1) 0%,
        rgba(247, 247, 247, 0) 18%
      );
      img {
        width: 100%;
        max-width: 100%;
        height: 100%;
      }
    }

    &-front {
      position: absolute;
      width: 100%;
      height: 100%;
      transform: rotateY(0deg) translateZ(1px);
    }

    &-back {
      position: absolute;
      width: 100%;
      height: 100%;
      transform: rotateY(180deg) translateZ(1px);
    }
  }
  input[type="radio"] {
    display: none;
    &:checked + .book__page {
      transition: transform 0.9s cubic-bezier(0.645, 0.045, 0.355, 1);
      transform: rotateY(-180deg);
    }
  }
}
</style>
